//
// Created by sandeep dommaraju on 2/25/16.
//

//#include vector
#include<string.h>

/*
class StackOfTrees{

    public:
        Stack<Tree> treeStack;
        void push(vector<Tree> trees);
        vector<Tree> pop(int count);
        void buildTree(int count);


};

class Stack{

};

TreeNode convertToBinaryTree(MTreeNode* mroot){
    TreeNode root;
    MTreeNode* curr = mroot;
    while(curr != NULL){
        vector<MTreeNode> children = curr->children;
        TreeNode node = new TreeNode(curr->val);

    }
    return root;
}

class TreeNode{

    public:
        string val;
        TreeNode left;
        TreeNode right;
};

class MTreeNode{
    public:
        string val;
        vector<MTreeNode> children;

};
 */